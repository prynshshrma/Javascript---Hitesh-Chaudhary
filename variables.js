const accountId = 7484
let emailId = "priyansh@gmail.com"

let state; // it is treated as undefined as javascript

// accountId = 90 

// var must not be used because it is functional scoped rather than block scoped   this means if we change a variable declared with var it also affects all other same variables throughout the entire code

console.log(accountId);
console.table([accountId, emailId, state])

console.table([accountId, emailId, state])
